load("@rules_java//java:defs.bzl", "java_binary", "java_library", "java_test")

java_library(
    name = "testutil",
    srcs = glob(["nullpomino/testutil/*.java"]),
    javacopts = ["-encoding", "UTF-8"],
    visibility = ["//visibility:private"],
    deps = ["//src/main/java:nullpomino"],
)

java_test(
    name = "AllTests",
    srcs = glob(
        ["**/*.java"],
        exclude = ["nullpomino/testutil/*.java"],
    ),
    args = [
        "execute",
        "--select-package=nullpomino",
        "--select-package=net.clarenceho",
        "--select-package=org.cacas",
        "--disable-banner",
        "--fail-if-no-tests",
    ],
    data = [
        "//:config_files",
        "//src/test/resources:test_resources",
    ],
    javacopts = ["-encoding", "UTF-8"],
    main_class = "org.junit.platform.console.ConsoleLauncher",
    use_testrunner = False,
    deps = [
        ":testutil",
        "//src/main/java:nullpomino",
        "@maven//:org_junit_jupiter_junit_jupiter_api",
        "@maven//:net_java_dev_jna_jna",
    ],
    runtime_deps = [
        "@maven//:org_junit_jupiter_junit_jupiter_engine",
        "@maven//:org_junit_platform_junit_platform_console",
    ],
)

java_binary(
    name = "BaselineRecorder",
    main_class = "nullpomino.testutil.BaselineRecorder",
    runtime_deps = [":testutil"],
)

java_binary(
    name = "CorpusGenerator",
    main_class = "nullpomino.testutil.CorpusGenerator",
    runtime_deps = [":testutil"],
)
